#version 330 core
in vec3 FragPos;  // Receive from vertex shader

uniform vec3 objectColor;  // Color of the object

out vec4 FragColor;

void main()
{
    FragColor = vec4(objectColor, 1.0);  // Set the fragment color using objectColor
}
